clc; clear; close all;

%% Load the Driving Cycle Current Data
Batt = readtable("../data/driving_profile-1.csv","VariableNamingRule","preserve");
Current = Batt(:,"Current (A)");
T = Batt(:,"Time (s)");

%% Call Function
[Voltage, SoC] = OCVRRC_SS(Current);

%% Plot 
subplot(3,1,1)
plot(Voltage)

subplot(3,1,2)
plot(SoC)

subplot(3,1,3)
plot(Current)

%%
load ../data/soc_ocv_profile-1.mat